[
  {
    "name": "db-migrate",
    "image": "${image}",
    "command": ["bundle", "exec", "rake", "db:migrate"],
    "memory": 300,
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "${log_group}",
        "awslogs-region": "us-east-1",
        "awslogs-stream-prefix": "db_migrate"
      }
    },
    "environment": [
      {
        "name": "ADMIN_EMAIL",
        "value": "sam@enterpriseoss.dev"
      },
      {
        "name": "BASE_URL",
        "value": "${base_url}"
      },
      {
        "name": "RACK_ENV",
        "value": "production"
      },
      {
        "name": "DATABASE_URL",
        "value": "${database_url}"
      },
      {
        "name": "SESSION_SECRET",
        "value": "${session_secret}"
      },
      {
        "name": "PORT",
        "value": "80"
      },
      {
        "name": "SMTP_LOGIN",
        "value": "${smtp_login}"
      },{
        "name": "SMTP_PASSWORD",
        "value": "${smtp_password}"
      },{
        "name": "SMTP_DOMAIN",
        "value": "${smtp_domain}"
      },{
        "name": "SMTP_SERVER",
        "value": "${smtp_server}"
      },{
        "name": "SMTP_PORT",
        "value": "25"
      }
    ]
  }
]